import React from 'react'

const TableHeadBar = () => {
  return (
    <section className='w-full h-12 bg-primary text-white flex justify-between items-center'>
        <div>qualified & disqualified buttons</div>

        <div>date</div>

        <div>search com</div>
    </section>
  )
}

export default TableHeadBar